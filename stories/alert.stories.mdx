import hbs from 'htmlbars-inline-precompile'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

# Hello there

<Meta title="Alert" />

The purpose of this `AlertBanner` (yeah it's not `Alert`) component is to do a thing.

<Preview>
  <Story name="standard">
{{
    template: hbs`<AlertBanner @message={{message}} @title={{title}} />`,
  context: {
    message: "this is a message",
    title: "Warning"
  }
}}
  </Story>
</Preview>

<Props of="AlertBanner" />
